
#basic_auth admin bcrypt:$2a$14$TzL7qrw.E4ZZaO7Wz2M.Ye7fB0.L0tN7yqQ2M6t0e1p5u4v3r2s1t


:8004 {
  forward_proxy {
    # 仅指定允许连接的目标端口范围（可选，但推荐设置）
    # 这里允许常见的 HTTP (80) 和 HTTPS (443) 端口
  }
  # 注意：这里没有 reverse_proxy 或其他指令
  # forward_proxy 会直接处理代理请求
}